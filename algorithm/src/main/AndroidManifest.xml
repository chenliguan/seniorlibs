<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.seniorlibs.algorithm">

    <application
        android:allowBackup="true"
        android:icon="@drawable/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        tools:ignore="GoogleAppIndexingWarning">
        <activity android:name=".main.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name=".main.AlgorithmTestActivity" />

        <activity android:name=".linkedlist.LinkedActivity" />

        <activity android:name=".array.ArrayActivity" />

        <activity android:name=".binarytree.BinaryTreeActivity" />

        <activity android:name=".dfsbfs.DfsBfsActivity" />

        <activity android:name=".map.MapActivity" />

        <activity android:name=".stack.StackActivity" />

        <activity android:name=".queue.QueueActivity" />

        <activity android:name=".heap.HeapActivity" />

        <activity android:name=".recursive.RecursiveActivity" />

        <activity android:name=".db.DbActivity" />

        <activity android:name=".greedy.GreedyActivity" />

        <activity android:name=".binarysearch.BinarySearchActivity" />

        <activity android:name=".trie.TrieActivity" />

        <activity android:name=".bit.BitActivity" />
    </application>

</manifest>