println "baselib.gradle start"

apply plugin: 'com.android.library'

android {
    compileSdkVersion rootProject.ext.android.compileSdkVersion
    buildToolsVersion rootProject.ext.android.buildToolsVersion

    defaultConfig {
        minSdkVersion rootProject.ext.android.minSdkVersion
        targetSdkVersion rootProject.ext.android.targetSdkVersion
        versionCode rootProject.ext.android.versionCode
        versionName rootProject.ext.android.versionName

        testInstrumentationRunner rootProject.ext.dependence.test_android_runner
    }

    buildTypes {
        debug {
            minifyEnabled false
            proguardFiles getDefaultProguardFile('proguard-android.txt'), 'proguard-rules.pro'
        }
        release {
            minifyEnabled false
            proguardFiles getDefaultProguardFile('proguard-android.txt'), 'proguard-rules.pro'
        }
    }

    sourceSets {
        main {
            jniLibs.srcDirs = ['libs']
        }
    }

    allprojects {
        repositories {
            maven { url "https://jitpack.io" }
        }
    }

    compileOptions {
        sourceCompatibility = rootProject.ext.java.javaVersion
        targetCompatibility = rootProject.ext.java.javaVersion
    }

    packagingOptions {
        exclude 'META-INF/proguard/coroutines.pro'
    }
}

dependencies {
    api fileTree(dir: 'libs', include: ['*.jar'])

    // appcompat
    api rootProject.dependence.appcompat_v7
    api rootProject.dependence.support_v4
    api rootProject.dependence.support_v13
    api rootProject.dependence.recyclerview_v7
    api rootProject.dependence.constraintlayout
    api rootProject.dependence.palette
    api rootProject.dependence.cardview
    api rootProject.dependence.material
    api rootProject.dependence.viewpager2
    api rootProject.dependence.drawerlayout
    api rootProject.dependence.fragment

    // kotlin
    api rootProject.dependence.kotlin_stdlib_jdk7
    api rootProject.dependence.kotlin_core

    // glide
    api rootProject.dependence.glide
    annotationProcessor rootProject.dependence.glide_annotation
    api rootProject.dependence.imageloader

    // okhttp
    api rootProject.dependence.okhttp
    api rootProject.dependence.okio
    // retrofit
    api rootProject.dependence.retrofit2
    api rootProject.dependence.retrofit2_gson
    api rootProject.dependence.retrofit2_rxjava2
    // rxJava
    api rootProject.dependence.rxandroid2
    api rootProject.dependence.rxjava2
    // fastJson
    api rootProject.dependence.fastjson
    // EventBus
    api rootProject.dependence.eventbus
    // vlayout
    api rootProject.dependence.vlayout

    // lifecycle
    api rootProject.dependence.lifecycle_runtime
    api rootProject.dependence.lifecycle_common
    api rootProject.dependence.lifecycle_extensions
    // viewModel
    api rootProject.dependence.lifecycle_viewmodel
    // liveData
    api rootProject.dependence.lifecycle_livedata
    api rootProject.dependence.fragment

    // 功能
    api rootProject.dependence.mmkv
    api rootProject.dependence.multidex
}


println "baselib.gradle end"