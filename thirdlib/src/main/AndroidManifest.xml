<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.seniorlibs.thirdlib">

    <application
        android:name="com.seniorlibs.thirdlib.MyApplication"
        android:allowBackup="true"
        android:icon="@drawable/ic_launcher"
        android:label="@string/app_name"
        android:screenOrientation="portrait"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        tools:ignore="GoogleAppIndexingWarning"
        tools:replace="android:name">

        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name=".retrofit.GankActivity" />

        <activity android:name=".rxjava.RxTestActivity" />

        <activity android:name=".rxjava.RxJavaActivity" />

        <activity android:name=".rxjava.RxCreateActivity" />

        <activity android:name=".rxjava.RxMapActivity" />

        <activity android:name=".rxjava.RxMergeActivity" />

        <activity android:name=".rxjava.RxFunctionActivity" />

        <activity android:name=".rxjava.RxFilterActivity" />

        <activity android:name=".rxjava.RxConditionsActivity" />

        <activity android:name=".eventbus.EventBusActivity" />

        <activity android:name=".okhttp.OkHttpActivity" />
    </application>

</manifest>